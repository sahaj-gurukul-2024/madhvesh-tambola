class Game {
    fun validate(ticket: Ticket, announcedNumbers: List<Int>, claim: String)
    {
        announcedNumbers.forEach() {announceValue ->
            iterateThrough(announceValue)
        }
    }

    fun iterateThrough(announceValue: Int) {

    }

}